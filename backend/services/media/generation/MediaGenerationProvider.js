export class MediaGenerationProvider {
  async generate({
    campaign,
    content,
    mediaType,
  }) {
    throw new Error(
      "MediaGenerationProvider.generate nao implementado."
    );
  }
}
